import { attachPropertiesToComponent } from 'an-mobile/utils/attach-properties-to-component';
import { IndexBar } from './IndexBar';
// import { Panel } from './Panel';

import './IndexBar.less';

attachPropertiesToComponent(IndexBar, {
  // Panel: Panel,
});
