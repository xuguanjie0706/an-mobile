import { defineConfig } from 'dumi';

export default defineConfig({
  outputPath: 'docs-dist',
  base:"/an-mobile/",
  themeConfig: {
    name: 'an-mobile',
    logo: false,
    // deviceWidth: 375,
  },
});
